{
  "name": "openpolitics-node-extension",
  "version": "0.1.0",
  "private": true,
  "description": "OpenPolitics volunteer IT-wing node — MV3 browser extension that captures PUBLIC, logged-out Instagram data and submits to the tenant coordinator.",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  }
}
