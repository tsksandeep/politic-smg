# Root .env — compose-level interpolation only (read by `docker compose`, NOT by the Edge Functions).
# Edge Function / media-worker secrets live in .env.local (see .env.local.example).
# Copy to .env if you want to override the committed local-dev defaults.

# Public anon key the frontend container is built with (local-dev demo value; safe to commit).
SUPABASE_LOCAL_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0"

# First-run seeding of the demo tenants (demo_tenant.sql). Set to false to bring the stack up empty.
SEED=true
